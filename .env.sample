# Copy to .env and edit if needed when running docker-compose
# Frontend build args (Vite variables baked at build time)
VITE_REDIRECT_URI=https://172.19.16.22:8001
VITE_API_HOST=172.19.16.22
VITE_API_PORT=8000

# Backend / Redis
REDIS_HOST=redis
REDIS_PORT=6379
